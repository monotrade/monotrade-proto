syntax = "proto3";

//import "google/protobuf/any.proto";


package monotrade;

/**
 *
 */
 
 enum OrderErrorCode {
    UNKNOWN = 0;
    UNSUPPORTED_PRICE_TYPE = 1;
    BAD_DATA_FORMAT = 2;
  }
  



  enum RiskLevel {
    NORMAL = 0;
    WARNING = 1;
    FORBIDDEN = 2;
  }
  

message AddGatewayCommand {
  string gatewayID = 1;
  string gatewayName = 2;
  any configuration = 3;
}

service Gateway {
    //rpc Connect(Any) returns (SearchResponse);
}